ext {
    //说明：
    //在本文件中编辑SDK版本号、上报地址、编译JDK版本信息，、
    //使用 makeJarH(HTTP版本）和makeJarL(LwM2M版本）进行编译，
    //编译成功后会在根目录SDKPackage目录下生成对应目录


    url = [
            "http" : "http://192.168.1.142:8090",
            "lwm2m": "coap://172.16.168.88:5683",
    ]

    lwm2m_sdkVersion = "3.3.1.3"
    http_sdkVersion = "3.3.1.3"

    jdkVersion = JavaVersion.VERSION_1_8

}